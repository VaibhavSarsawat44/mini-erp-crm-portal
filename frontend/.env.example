# API Base URL (optional: only if not using Vite dev server proxy)
# For local setup, the proxy in vite.config.ts automatically routes /api to http://localhost:5001
VITE_API_URL=http://localhost:5001
